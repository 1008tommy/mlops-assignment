# Streamlit runtime config for GCP Cloud Run.
# Cloud Run serves the container on port 8080 by default ($PORT env var).
[server]
headless = true
address = "0.0.0.0"
port = 8080

[browser]
gatherUsageStats = false
